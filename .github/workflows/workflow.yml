name: hello world!
on: [workflow_dispatch]

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: repostory clonation
              uses: actions/checkout@v3
            - run: python3 main.py
            - run: ls -la

